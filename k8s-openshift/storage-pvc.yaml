apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: order-service-pvc
  namespace: my-app-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gp3-csi